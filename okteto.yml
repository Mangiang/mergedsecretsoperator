name: mergedsecret
image: okteto/python:3
namespace: mergedsecrets
command: bash
securityContext:
  capabilities:
    add:
      - SYS_PTRACE
serviceAccount: merged-secrets
sync:
  - .:/usr/src/app
autocreate: true
